import 'package:flutter/material.dart';

class MainBottomAd extends StatelessWidget {
  const MainBottomAd({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.grey.shade200,
      height: 80,
    );
  }
}
